using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using RTS;

public class SpaceStation : Base {
    //protected override void Start () {
    //    base.Start();
    //    actions = new string[] { "Spaceship" };
    //    //each action we want to perform is the name of a unit to that can be constructed
    //    //these names will need to match the names of any prefabs that we are wanting to build 
    //    //and match perfectly, since things are case-sensitive here
    //}
    //public override void PerformAction(string actionToPerform)
    //{
    //    base.PerformAction(actionToPerform);
    //    CreateUnit(actionToPerform);
    //}
    protected override void Start () 
    {
       base.Start();
    }
    protected override void Update()
    {
        base.Update();
    }
}

